# runtime サブパッケージ

<!-- README_LEVEL: L3 -->

## 目的

実行時ユーティリティ（設定ロード、ジョブ実行、可視化連携）をまとめるレイヤです。

## 含まれる要素

- `config.py`: 設定読み込み・検証
- `jobs.py`: ジョブのスケジューリング・ディスパッチ
- `visuals.py`: シミュレーション表示の補助

## 更新ルール

- `main()` 側は本パッケージを薄く結合するだけに保つ
- 設定処理と実行処理を分離し、単体テスト可能な構成を維持する
